SELECT DISTINCT ?item ?itemLabel ?imdb ?genderLabel WHERE {
  ?item wdt:P106 wd:Q2526255;
  wdt:P21 ?gender.
  OPTIONAL { ?item wdt:P345 ?imdb. }
  SERVICE wikibase:label { bd:serviceParam wikibase:language "en". }
}